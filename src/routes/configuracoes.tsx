import { createFileRoute } from "@tanstack/react-router";
import { Bell, Building2, Cable, LockKeyhole, Save, ShieldCheck, User, Users } from "lucide-react";
import { useState } from "react";
import { Button, Card, Field, Input, Select } from "../components/ui";
import { PageHeader } from "../components/page-parts";
const sections = [{ id: "Empresa", icon: Building2 }, { id: "Usuários", icon: Users }, { id: "Permissões", icon: ShieldCheck }, { id: "Segurança", icon: LockKeyhole }, { id: "Integrações", icon: Cable }, { id: "Notificações", icon: Bell }, { id: "Perfil", icon: User }];
export const Route = createFileRoute("/configuracoes")({ head: () => ({ meta: [{ title: "Configurações — Automy" }, { name: "description", content: "Preferências e configurações da Automy." }, { property: "og:title", content: "Configurações — Automy" }, { property: "og:description", content: "Preferências e configurações da Automy." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: SettingsPage });
function SettingsPage() { const [active, setActive] = useState("Empresa"); const [saved, setSaved] = useState(false); return <div><PageHeader title="Configurações" description="Gerencie preferências da empresa, acesso e integrações." /><div className="grid gap-6 lg:grid-cols-[220px_minmax(0,1fr)]"><nav className="flex gap-1 overflow-x-auto lg:flex-col">{sections.map((s) => <button key={s.id} onClick={() => {setActive(s.id); setSaved(false)}} className={active === s.id ? "flex shrink-0 items-center gap-3 rounded-lg bg-accent px-3 py-2.5 text-sm font-medium text-foreground" : "flex shrink-0 items-center gap-3 rounded-lg px-3 py-2.5 text-sm text-muted-foreground hover:bg-accent/60 hover:text-foreground"}><s.icon className="size-4" />{s.id}</button>)}</nav><Card className="p-6"><div className="border-b border-border pb-5"><h2 className="font-semibold">{active}</h2><p className="mt-1 text-sm text-muted-foreground">Configure as preferências de {active.toLowerCase()} da organização.</p></div>{active === "Empresa" || active === "Perfil" ? <form className="mt-6 grid max-w-2xl gap-5" onSubmit={(e) => {e.preventDefault(); setSaved(true)}}><div className="grid gap-5 sm:grid-cols-2"><Field label={active === "Empresa" ? "Razão social" : "Nome completo"}><Input defaultValue={active === "Empresa" ? "Automy Sistemas Ltda." : "Marina Costa"} /></Field><Field label={active === "Empresa" ? "CNPJ" : "E-mail"}><Input defaultValue={active === "Empresa" ? "42.845.991/0001-02" : "marina@automy.com.br"} /></Field></div><Field label="Fuso horário"><Select><option>América/São Paulo (UTC-3)</option><option>UTC</option></Select></Field><div className="flex items-center gap-3"><Button><Save className="size-4" />Salvar alterações</Button>{saved && <span className="text-sm text-success">Alterações salvas.</span>}</div></form> : <div className="mt-6 space-y-3">{["Acesso administrativo", "Alertas por e-mail", "Confirmação em duas etapas"].map((item, index) => <label key={item} className="grid grid-cols-[minmax(0,1fr)_auto] items-center gap-4 rounded-lg border border-border p-4"><div><div className="text-sm font-medium">{item}</div><div className="text-xs text-muted-foreground">Controle esta preferência para todos os usuários.</div></div><input type="checkbox" defaultChecked={index !== 1} className="size-4 accent-primary" /></label>)}</div>}</Card></div></div>; }