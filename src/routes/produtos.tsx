import { createFileRoute } from "@tanstack/react-router";
import { Plus } from "lucide-react";
import { useState } from "react";
import { Badge, Button, Field, Input, Modal, Pagination, SearchInput, Select } from "../components/ui";
import { PageHeader, TableShell, td, th } from "../components/page-parts";
import { products, toneFor } from "../lib/mock-data";
export const Route = createFileRoute("/produtos")({ head: () => ({ meta: [{ title: "Produtos — Automy" }, { name: "description", content: "Portfólio de produtos Automy." }, { property: "og:title", content: "Produtos — Automy" }, { property: "og:description", content: "Portfólio de produtos Automy." }, { property: "og:type", content: "website" }, { name: "twitter:card", content: "summary_large_image" }] }), component: Products });
function Products() { const [search, setSearch] = useState(""); const [open, setOpen] = useState(false); const rows = products.filter((p) => p.name.toLowerCase().includes(search.toLowerCase())); return <div><PageHeader title="Produtos" description="Gerencie o portfólio de soluções oferecidas aos clientes." action={<Button onClick={() => setOpen(true)}><Plus className="size-4" />Novo produto</Button>} /><div className="mb-4"><SearchInput value={search} onChange={setSearch} placeholder="Buscar produto..." /></div><TableShell footer={<Pagination />}><table className="w-full"><thead><tr>{["Nome", "Categoria", "Versão", "Clientes utilizando", "Status"].map((h) => <th key={h} className={th}>{h}</th>)}</tr></thead><tbody>{rows.map((p) => <tr key={p.name} className="hover:bg-muted/30"><td className={td}><div className="font-medium">{p.name}</div></td><td className={td}>{p.category}</td><td className={td}><code className="rounded bg-muted px-2 py-1 text-xs">{p.version}</code></td><td className={td}>{p.clients} clientes</td><td className={td}><Badge tone={toneFor(p.status)}>{p.status}</Badge></td></tr>)}</tbody></table></TableShell><Modal open={open} onClose={() => setOpen(false)} title="Novo produto"><form className="grid gap-4" onSubmit={(e) => {e.preventDefault(); setOpen(false)}}><Field label="Nome"><Input required /></Field><Field label="Categoria"><Select><option>Automação</option><option>Analytics</option><option>Atendimento</option></Select></Field><div className="flex justify-end gap-2"><Button type="button" variant="secondary" onClick={() => setOpen(false)}>Cancelar</Button><Button>Salvar produto</Button></div></form></Modal></div>; }