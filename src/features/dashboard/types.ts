export type ClientGrowthPoint = {
  month: string;
  active: number;
  onboarding: number;
};

export type RevenueGrowthPoint = {
  month: string;
  revenue: number;
};

export type Activity = {
  title: string;
  meta: string;
};
